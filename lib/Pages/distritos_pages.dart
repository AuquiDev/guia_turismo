import 'package:flutter/material.dart';

class DistritoPage extends StatelessWidget {
  const DistritoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Distrito pagina')),
    );
  }
}