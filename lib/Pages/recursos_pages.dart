import 'package:flutter/material.dart';

class RecursosPages extends StatelessWidget {
  const RecursosPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page Recursos'),
      ),
    );
  }
}